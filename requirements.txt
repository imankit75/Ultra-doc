# ================================================================
# Ultra Doc-Intelligence — Python Dependencies
#
# Run the smart installer (auto-detects CPU vs GPU):
#   python install.py
#
# Or install manually for your platform:
#   CPU:  pip install -r requirements_cpu.txt
#   GPU:  pip install -r requirements_gpu.txt
# ================================================================
-r requirements_cpu.txt
