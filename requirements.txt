# ================================================================
# Ultra Doc-Intelligence — Python Dependencies
# Tested with Python 3.11 (conda env: ultra_doc)
# ================================================================

# --- LLM (local GGUF inference) ---
# For GPU support, reinstall with CUDA flags:
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --force-reinstall
llama-cpp-python>=0.3.16

# --- Document parsing ---
PyMuPDF>=1.27.0          # PDF parsing (import as fitz)
python-docx>=1.1.0       # DOCX parsing

# --- OCR (optional, for image documents) ---
pillow>=10.0.0           # Image loading (PIL)
pytesseract>=0.3.10      # Tesseract OCR wrapper
                         # Also requires Tesseract binary installed on system:
                         #   Windows: https://github.com/UB-Mannheim/tesseract/wiki
                         #   Linux:   sudo apt-get install tesseract-ocr

# --- Embeddings + Vector store ---
sentence-transformers>=3.0.0
chromadb>=0.5.0

# --- Deep learning backend (for sentence-transformers) ---
# CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
# GPU: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0

# --- UI ---
gradio>=6.0.0

# --- Utilities ---
python-dotenv>=1.0.0
