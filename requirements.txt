# ================================================================
# Ultra Doc-Intelligence — Python Dependencies
# Python 3.11 | Tested on Windows (conda env)
#
# Install with:
#   pip install -r requirements.txt
#
# NOTE: llama-cpp-python must be installed separately via conda.
# See README.md for the full setup steps.
# ================================================================

# --- PyTorch (CPU-only — works on any machine, no GPU required) ---
# The +cpu tag ensures pip picks the CPU wheel and NOT the CUDA version.
# CUDA version causes a DLL error (shm.dll) on machines without an NVIDIA driver.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu

# --- Document parsing ---
PyMuPDF>=1.27.0          # PDF parsing (import as fitz)
python-docx>=1.1.0       # DOCX parsing

# --- OCR (optional — requires Tesseract binary on system) ---
pillow>=10.0.0
pytesseract>=0.3.10

# --- Embeddings + Vector store ---
sentence-transformers>=3.0.0
chromadb>=0.5.0

# --- UI ---
gradio>=6.0.0

# --- Utilities ---
python-dotenv>=1.0.0

# ----------------------------------------------------------------
# llama-cpp-python is NOT listed here — it needs a C++ compiler
# to build from pip on Windows. Install via conda-forge instead:
#
#   conda install -c conda-forge llama-cpp-python -y
#
# See README.md Step 2 for details.
# ----------------------------------------------------------------
